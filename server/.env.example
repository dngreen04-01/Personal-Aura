# Gemini AI API key
GEMINI_API_KEY=

# Express server port (Cloud Run uses 8080)
PORT=3001

# API key for Cloud Scheduler job authentication
JOBS_API_KEY=

# Environment (development | production)
NODE_ENV=development

# CORS origin restriction (leave empty for development, set to production URL in prod)
CORS_ORIGIN=
